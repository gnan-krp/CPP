# C++ Practice Programs

This repository contains **10 C++ programs** covering beginner to moderate-level topics.  
Each program demonstrates key C++ concepts like arrays, strings, recursion, functions, structures, pointers, patterns, and matrix operations.

---

## 📂 Folder & Program List

1. **1D_Array_Frequency** – Count frequency of each element in a 1D array.  
2. **2D_Array_MatrixMultiplication** – Multiply two matrices entered by the user.  
3. **String_Palindrome** – Check if a string is a palindrome ignoring spaces and case.  
4. **Recursion_SumOfDigits** – Find the sum of digits of a number using recursion.  
5. **Function_SwapNumbers** – Swap two numbers using a function with pointers.  
6. **Structure_StudentAverage** – Calculate average marks of a student using a structure.  
7. **PointerToStructure_Point** – Modify structure members using a pointer to structure.  
8. **PointerToFunction_Calculator** – Simple calculator using function pointers.  
9. **Pattern_Diamond** – Print a diamond star pattern.  
10. **Unique_MatrixTranspose** – Find the transpose of a 3x3 matrix using a function.

---

## ⚙️ How to Build & Run

### Using g++ (Command Line)
1. Navigate to program folder, e.g., `1D_Array_Frequency/`
2. Compile:
g++ main.cpp -o program
3. Run:
./program (Linux/macOS)
program.exe (Windows)

### Using CMake (Recommended)
1. From repository root:
mkdir build
cd build
cmake ..
cmake --build .
2. Run any executable from `build/` folder.

---

## 📌 Tips
- Each program is **self-contained** and uses `cin` and `cout`.  
- Variables and functions are **clearly named**.  
- Programs are a good **practice set for beginners** learning C++ basics and intermediate concepts.

